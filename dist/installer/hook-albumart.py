import oshiddenimports = ["sip"]for f in os.listdir("../../lib/albumart/"):    if (f.startswith("albumart_source") or \        f.startswith("albumart_target") or \        f.startswith("albumart_recognizer")) and f.endswith(".py"):          hiddenimports.append(os.path.basename(f).split(".")[0])print "Found modules:", hiddenimports